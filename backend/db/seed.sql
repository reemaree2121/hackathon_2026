-- ---------------------------------------------------------------------------
-- DEPRECATED: this static seed file has been replaced.
--
-- Realistic seed data (200+ students, 40+ faculty, full timetables, 25+
-- events, 40+ senior mentors, clubs, campus locations, notices, books, and
-- resources) is now generated by db/seed-realistic.js, which populates every
-- table programmatically with far more coverage than a static SQL file could
-- reasonably hold.
--
-- After running schema.sql, seed the database with:
--   cd backend
--   npm install
--   npm run seed
-- (or: node db/seed-realistic.js)
--
-- This file is kept only so old references to seed.sql don't 404; it does
-- not need to be run.
-- ---------------------------------------------------------------------------
